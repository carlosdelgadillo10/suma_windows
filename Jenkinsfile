pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                // Checkout the code from the Git repository
                git url: 'https://github.com/carlosdelgadillo10/suma-windows.git', branch: 'main'
            }
    }
}
